version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.15
    working_directory: /app
    steps:
      - checkout
      - run:
          name: Setup Dependencies
          command: npm install
      - run:
          name: test
          command: npm test
